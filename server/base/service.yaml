apiVersion: v1
kind: Service
metadata:
  name: chocoletter-server-base-service
  namespace: chocoletter
spec:
  type: ClusterIP
  selector:
    app: chocoletter-server-base
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
