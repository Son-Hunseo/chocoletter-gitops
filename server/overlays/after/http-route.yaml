apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: chocoletter-server-after-route
  namespace: chocoletter
spec:
  parentRefs:
    - name: web-gateway
      namespace: nginx-gateway
      sectionName: http-wildcard
    - name: web-gateway
      namespace: nginx-gateway
      sectionName: https-wildcard
  hostnames:
    - chocoletter-server-after.sonhs.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: chocoletter-server-after-service
          port: 80
