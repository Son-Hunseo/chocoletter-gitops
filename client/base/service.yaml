apiVersion: v1
kind: Service
metadata:
  name: chocoletter-client-base-service
  namespace: chocoletter
spec:
  selector:
    app: chocoletter-client-base
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
